sudo: required

services:
  - docker

before_install:
  - cd client && docker build -t moumenh/client .
  - cd Backend && docker build -t moumenh/back .

after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push moumenh/client
  - docker push moumenh/back

